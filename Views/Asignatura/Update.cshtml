@model Asignatura

@{
    ViewData["Title"] = "Actualización de Asignatura";
    Layout = "_Simple";
}

<h1>@ViewData["Title"]</h1>
<h2>@ViewBag.IdAsignaturaAEditar</h2>

<div asp-validation-summary="All"></div>
<form method="POST">
    <div>
        <label asp-for="Nombre"></label>
        <input asp-for="Nombre"/>
    </div>
    <div>
        <label asp-for="CursoId"></label>
        @Html.DropDownListFor(
            x=>x.CursoId, 
            (IEnumerable<SelectListItem>)ViewBag.idCursosDisponibles,
            "Seleccione un curso",
            new { @class = "form-control" }
        )
    </div>
    <input type="submit" value="Actualizar" class="btn btn-warning"/>
    @Html.ActionLink("Cancelar", "MultiAsignatura", "Asignatura",null, new{@class="btn btn-primary"})
</form>


<p><strong>@ViewData["fecha"]</strong></p>